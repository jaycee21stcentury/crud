const prompt = require("prompt-sync");
let calculate = 'yes';
while(calculate === 'yes'){
    console.log('Calculator');

    let Calculator = prompt("Enter the operation you want to perform (+,-,*,/")
}